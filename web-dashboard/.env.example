# Base URL of the Cloud Server the dashboard talks to.
# Dev: leave blank to use the Vite proxy (/v1 -> http://localhost:8002).
# Prod: set to the deployed cloud URL, e.g. https://api.yourdomain.com
# (the server's CORS_ORIGINS must include this dashboard's origin).
VITE_CLOUD_URL=
